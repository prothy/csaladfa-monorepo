{
  "name": "root",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:client": "pnpm --filter client dev",
    "dev:server": "pnpm --filter server dev",
    "dev": "pnpm --parallel dev",
    "build": "pnpm -r build"
  }
}
